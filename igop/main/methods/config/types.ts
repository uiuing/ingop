export type IsWin = boolean

export type IGopPaths = {
  home: string
  // go+ files
  gopRoot: string
  gopBin: string
  // env for go files
  env: string
  goRoot: string
  goPath: string
  goBin: string
}
