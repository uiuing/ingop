export type IsWin = boolean

export type IngopPaths = {
  home: string
  // go+ files
  gopRoot: string
  gopBin: string
  // env for go files
  env: string
  goRoot: string
  goPath: string
  goBin: string
}
